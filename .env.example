# Optional presentation gate for protected Work pages.
# This is client-side only and must not be treated as secure access control.
PUBLIC_WORK_GATE_PASSWORD=replace-me

# Optional local override. Production builds use the deployed Azure endpoint.
PUBLIC_AADITH_CHAT_ENDPOINT=http://localhost:7071/api/chat
