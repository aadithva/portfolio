{
  "name": "HoverRevealList",
  "path": "src/components/molecules/HoverRevealList.astro",
  "tier": "molecule",
  "summary": "Numbered work index with a persistent left thumbnail and orange pixel swaps.",
  "what": "Maps `projects` to linked rows (index, title, category, year) beside a static media column; fine-pointer hover or keyboard focus updates that thumbnail while PixelTransition covers each image swap in orange.",
  "why": "Keeps the homepage and Work index scannable while giving imagery a stable grid position instead of letting previews float over content.",
  "whenToUse": [
    "List work projects on the home and Work pages.",
    "Show an image-led index that rewards hover without depending on it."
  ],
  "whenNotToUse": [
    "Do not use for Lab projects \u2014 use LabCard grid.",
    "Do not duplicate the row markup elsewhere."
  ],
  "propsOrInputs": [
    {
      "name": "projects",
      "type": "Project[]",
      "required": true
    },
    {
      "name": "showViewAll",
      "type": "boolean",
      "required": false
    }
  ],
  "componentDependencies": [
    "PixelTransition",
    "Reveal"
  ],
  "dataDependencies": [],
  "usedBy": [
    "FeaturedWork",
    "route /work"
  ],
  "a11yNotes": [
    "Each row remains a single anchor with readable text order.",
    "The persistent preview is decorative; coarse pointers retain the first static thumbnail.",
    "Missing thumbnails use an explicit visual placeholder until the configured asset arrives."
  ],
  "version": "0.3.0",
  "lastUpdated": "2026-07-10",
  "source": "Generated from Astro source + design-system/ai/index.json by component-metadata.py."
}
